<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="home.dao.UserDao">  
<insert id="add" parameterType="home.domain.User" flushCache="true">  
   INSERT INTO t_user (id,name,password) VALUES (#{id},#{name},#{password})  
</insert>  
<delete id="delete" parameterType="int">  
    DELETE FROM t_user WHERE id=#{id}  
</delete>  
<update id="update" parameterType="home.domain.User">  
    UPDATE t_user SET password=#{password} WHERE id=#{id}  
</update>  
<select id="query" parameterType="int" resultType="home.domain.User">  
    SELECT * FROM t_user WHERE id=#{id}  
</select>
<select id="queryByName" parameterType="java.lang.String" resultType="home.domain.User">  
    SELECT * FROM t_user WHERE name=#{name}  
</select>

<insert id="insertSelective" parameterType="home.domain.User">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue May 31 13:50:23 CST 2016.
    -->
    insert into t_user
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        ID,
      </if>
      <if test="name != null">
        name,
      </if>
      <if test="password != null">
        password,
      </if>
      <if test="roleId != null">
        roleId,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=INTEGER},
      </if>
      <if test="name != null">
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="password != null">
        #{password,jdbcType=VARCHAR},
      </if>
      <if test="roleId != null">
        #{roleId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>

<!-- 	<select id="queryUserByPage" parameterType="UserDto" resultType="home.domain.User">
	    <![CDATA[
		select * from t_user t where t.name = #{name}
		]]>
	</select> -->


</mapper>