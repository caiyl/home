<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="home.dao.UserDao">  
<select id="getUser" parameterType="home.domain.User" resultType="home.domain.User">  
    SELECT * FROM t_user WHERE name=#{name} AND password=#{password}  
</select>  
<insert id="addUser" parameterType="home.domain.User" flushCache="true">  
   INSERT INTO t_user (id,name,password) VALUES (#{id},#{name},#{password})  
</insert>  
<update id="updateUser" parameterType="home.domain.User">  
    UPDATE t_user SET password=#{password} WHERE id=#{id}  
</update>  
<delete id="deleteUser" parameterType="int">  
    DELETE FROM t_user WHERE id=#{id}  
</delete>  
</mapper>