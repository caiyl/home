<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="home.dao.UserDao">  
<insert id="add" parameterType="home.domain.User" flushCache="true">  
   INSERT INTO t_user (id,name,password) VALUES (#{id},#{name},#{password})  
</insert>  
<delete id="delete" parameterType="int">  
    DELETE FROM t_user WHERE id=#{id}  
</delete>  
<update id="update" parameterType="home.domain.User">  
    UPDATE t_user SET password=#{password} WHERE id=#{id}  
</update>  
<select id="query" parameterType="int" resultType="home.domain.User">  
    SELECT * FROM t_user WHERE id=#{id}  
</select>  
</mapper>